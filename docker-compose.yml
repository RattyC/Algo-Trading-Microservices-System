
version: '3.8'

services:
  # 🐰 RabbitMQ: ตัวกลางส่งข้อความ
  rabbitmq:
    image: rabbitmq:3-management
    container_name: algo_rabbitmq
    ports:
      - "5672:5672"   # Port สำหรับ Code เชื่อมต่อ
      - "15672:15672" # Port สำหรับหน้าเว็บจัดการ
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - algo-net

  # 🍃 MongoDB: ฐานข้อมูล
  mongo:
    image: mongo
    container_name: algo_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - algo-net

networks:
  algo-net:

volumes:
  mongo_data: